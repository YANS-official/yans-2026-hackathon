# YANS 2026 ハッカソン レギュレーション（参加規定）

本文書は、引用妥当性判定タスクの両部門に共通する規定と部門別の規定を定める。
提出ファイルの形式規定（§3）は提出サーバが自動検査し、違反する提出はエラーメッセージとともに受理されない。
判断に迷う場合は運営に事前確認すること。

## 定義

| 名前 | 定義 |
|------|------|
| Task 1（OMD） | 独自モデル部門（Own Model Development）。参加者が判定モデルを自作する部門 |
| Task 2（ICL） | 事例設計部門（In-Context Learning）。運営が固定した LLM に与える few-shot 事例を設計する部門 |
| 配布データ | 運営が配布する `data/` 配下のファイル一式（`papers.jsonl` / `train.jsonl` / `dev_labeled.jsonl` / `dev_leaderboard.jsonl` / `test.jsonl`） |
| リーダーボードフェーズ | `dev_leaderboard.jsonl`（100問）を対象とする日中の提出期間 |
| 最終フェーズ | `test.jsonl`（100問）を対象とする提出期間。17:30 に test の入力を公開し、18:00 に提出を締め切る |
| 提出サーバ | 運営が提供するリーダーボードアプリ。提出受付・形式検査・採点を行う |

## 1. 共通規定（両部門）

1. 使用するデータは配布データに限る。外部データセットの持ち込みは不可。
2. 学習に用いる事例（Task 1）および few-shot 事例（Task 2）は、配布データ（そのまま、または加工したもの）と自作したものに限る。LLM による事例の生成・蒸留は可。

## 2. 提出回数とスコアの運用（両部門）

1. リーダーボードフェーズの提出は、1チームあたり**合計30回まで**、かつ**直近60分間で6回まで**。
   形式検査に不合格となった提出は受理されず、回数には数えない。
2. リーダーボードには各チームの**最高スコアの提出**を表示する。
3. 最終フェーズの提出は 1 チームあたり**合計5回まで**。基本は1回の提出を前提とし、
   形式エラー等による再提出のための上限である。最終順位は、締切までに**最後に受理された提出**で
   決定する（受理＝形式検査に合格した提出。新しい提出が受理されるたびに、それ以前の提出は
   最終結果として扱われなくなる）。5回を超える提出が必要になった場合は運営に連絡すること
   （必要に応じて運営が対応する）。
4. Task 2 の採点は、提出ごとに**1回の実行**で行い、その結果を採用する。
   temperature=0・seed 固定でも LLM の実行結果は完全には再現されないため、
   スコアには実行に由来するばらつき（運営実測で最大 ±3.5 ポイント）が含まれることを前提とする。

## 3. 提出ファイルの形式規定（提出サーバが自動検査）

提出は JSONL ファイル（1行1サンプル）で行う。両部門とも、`id` は評価対象データの `id` 集合と
完全に一致すること（欠落・余分・重複があるとエラー）。

### Task 1（予測ファイル）

```json
{"id": "dev-001", "prediction": 1}
```

- 各行に `id` と `prediction` が必要
- `prediction` は整数の 0 または 1（真偽値 `true`/`false` は不可）

### Task 2（事例セットファイル）

```json
{"id": "dev-001", "exemplars": [
  {"citation_context": "... [CITE] ...", "cited_paper_id": "Vol21No05_02", "label": 1}
]}
```

- 各行に `id` と `exemplars` が必要
- `exemplars` は**最大5件**（0件 = そのサンプルは例示なしで判定）
- 各事例には `citation_context` / `cited_paper_id` / `label` の3フィールドが必要
- `citation_context` は **500文字以内**（全角・半角の区別なく Unicode 文字数で数える。Python `len()` 準拠）で、
  引用位置プレースホルダ **`[CITE]` をちょうど1個**含むこと
- `cited_paper_id` は配布 `papers.jsonl` に存在する ID のみ
- `label` は整数の 0 または 1（真偽値は不可）

## 4. Task 1（OMD・独自モデル部門）の規定

1. 使用できるモデルは、1つあたり**パラメータ数 100M 以下**に限る
   （modernbert-ja-70m 70.3M・ruri-v3-70m 70.0M 等は収まる。multilingual-e5-large 560M・BERT-base 110M は超過のため不可）。
   - 構成する各モデルが 100M 以下であれば、複数モデルの組み合わせ（アンサンブル・パイプライン）は可
   - パラメータ数を確認できないモデル（商用 API 等）は使用不可
2. モデルの構築（学習・特徴量計算を含む）は、**Google Colab Pro の A100 GPU を基準として総実行時間1時間以内**で再現できること。
   - 独自の計算環境（研究室クラスタ・自宅 GPU・有償クラウド等）で学習した重みの持ち込み・呼び出しは不可
   - 公開されている事前学習済み重みからの fine-tuning は可
3. 評価対象データ（`dev_leaderboard` / `test`）へのラベル推定（擬似ラベル）を**学習に用いることは不可**
   （LLM 判定の実質的なパイプラインへの組み込みを防ぐ）。

## 5. Task 2（ICL・事例設計部門）の規定

1. 判定に用いる LLM（gpt-5.4-nano・temperature=0・seed 固定）とプロンプトテンプレートは
   **運営指定のものに固定**する（運営サーバで集中実行。参加者は変更不可）。
   - 事例セットの作成方法は自由（手作業・スクリプト・自前の LLM 利用のいずれも可）
   - 公式のスコアはリーダーボードと最終評価のみ（自前 API での事前検証は妨げないが記録には使わない）
2. 事例は §3 の形式規定（1サンプルあたり5件以下・500文字以内・`[CITE]` ちょうど1個 等）を満たすこと。
3. 事例のフィールドを通じてモデルの判定を誘導する工夫は、§3 の形式規定の範囲で**戦略として許容**する
   （どのような文字列が判定に効くかの探究も本部門の題材に含める）。
   ただし、**OpenAI の利用規約に違反する内容（安全機構の回避を試みる指示等）、
   および運営の実行ハーネス・採点の動作を妨害する内容は失格**とする。

## 6. 最終成果物と順守の確認

- 各チームの最終成果物は、次の2点とする（notebook・コード・モデル重みの提出は不要）。
  1. 最終フェーズの**提出ファイル（JSONL）**
  2. 取り組みを**3分で説明するスライド**（手法の概要・使用モデル・工夫した点。提出方法と締切は当日案内する）
- レギュレーションの順守は自己申告制とし、スライドでの説明と質疑に基づいて確認する。
  疑義がある場合は運営がチームに確認する。判断に迷うものは運営に事前に相談すること。
